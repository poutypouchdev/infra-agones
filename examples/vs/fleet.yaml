apiVersion: agones.dev/v1
kind: Fleet
metadata:
  name: vs-game-server-fleet
spec:
  replicas: 1
  scheduling: Packed

  # Not sure which strategy to use: https://agones.dev/site/docs/guides/fleet-updates/
  strategy:
    type: Recreate

  template:
    spec:
      ports:
        - name: default
          containerPort: 7777

      # The GameServer's Pod template
      template:
        spec:
          tolerations:
          - key: kubernetes.azure.com/scalesetpriority
            operator: Equal
            value: 'spot'
            effect: NoSchedule

          containers:
            - name: vs-game-server
              image: poutypouchgamesacrcentralus.azurecr.io/vs-server:0.1.13
              resources:
                requests:
                  memory: 1000Mi
                  cpu: 900m # Side car requests 30m with no limit and other azure bullshit taking space.
                limits:
                  memory: 2000Mi
                  cpu: 1000m

# Date:       CPU:      Mem:
# 8/29/2024   15-35m    130-150Mi
#
#
#
#
#
#
#
